# Fix List — 2026-05-11

## Bugs / Broken

### 1. Keyboard covers album picker input
When typing a new album name in AlbumPickerSheet, the keyboard covers the text input. Wrap sheet content in `KeyboardAvoidingView` with `behavior="padding"`.
- File: `components/AlbumPickerSheet.tsx`

### 2. Bookmark toggle — no unsave path
Bookmark icon fills on save but there's no way to unsave/unbookmark. Need toggle: tap filled bookmark → remove from `saved_recipes` + remove from all `recipe_collection_items` for that recipe. Update `saveMutation` to be a toggle mutation.
- File: `app/recipe/[id]/index.tsx`

### 3. Auto-save when adding to album
When a recipe is added to any album via the picker, also insert into `saved_recipes` if not already saved. Currently you can add to an album without bookmarking, which is inconsistent.
- File: `components/AlbumPickerSheet.tsx` (PickAlbumsContent save function)

### 4. Bookmark icon not reflecting saved state on load
The `recipe-saved` query should check `saved_recipes` on mount and fill the bookmark accordingly. Verify this query is actually returning correct data — may be failing due to Supabase type inference.
- File: `app/recipe/[id]/index.tsx`

### 5. Likes not persisting / Favorites not showing
The like mutation may be silently failing (Supabase type `never` inference). Verify `recipe_likes` table exists in DB. Test the insert manually. The Liked/Favorites screen query may also need verification.
- Files: `app/recipe/[id]/index.tsx`, `app/liked.tsx`

### 6. DM thread doesn't scroll to bottom
FlatList shows oldest messages first, doesn't auto-scroll to newest. Use `inverted={true}` and reverse the data order, OR use `ref.scrollToEnd()` on mount. NOTE: This was already fixed via linter/user edit — FlatList now uses `inverted={true}` with `[...shares].reverse()`.
- File: `app/inbox/[userId].tsx`

### 7. DM emoji reactions don't dismiss on tap-away
Reactions only dismiss on scroll. Should also dismiss when tapping a different message card or any blank space. Clear `activeReactionId` on any card press. NOTE: Partially fixed — card onPress now clears activeReactionId before navigating.
- File: `app/inbox/[userId].tsx`

## UI Polish

### 8. Rename "Liked" → "Favorites"
Change the virtual album card label and the liked.tsx screen title from "Liked" to "Favorites".
- Files: `app/(tabs)/kitchen.tsx`, `app/liked.tsx`

### 9. Delete albums via long-press
Long-press album card in Kitchen → ActionSheet with "Delete album" (destructive). Delete from `recipe_collections` (cascades to items).
- File: `app/(tabs)/kitchen.tsx`

### 10. Inbox icon — replace emoji with Ionicons outline + unread dot
Replace the envelope emoji/text with `Ionicons` `mail-outline` (proper outlined icon). Add a small clay-colored dot badge on the icon when `inbox-unread` count > 0.
- File: `components/InboxIcon.tsx`

### 11. Search icon — make icon inside bigger
The search icon in Kitchen search bar is too small. Use `Ionicons` `search-outline` and bump size (e.g., 20→24) without changing the container size.
- File: `app/(tabs)/kitchen.tsx`

### 12. Profile edit button
Currently `onPress={() => {}}`. Wire to a profile edit modal or screen: change display_name, bio, avatar_url.
- File: `app/(tabs)/profile.tsx`

### 13. Settings page
Add settings screen accessible from profile (gear icon in header or row below palate card). Include:
- Unit preferences (metric vs US customary — see #16)
- Notification preferences
- Account settings
- Sign out (move from profile to settings)
- Any feature toggles
- File: new `app/settings.tsx`, update `app/(tabs)/profile.tsx`

## Ingredient System (V1)

### 14. Ingredient list redesign
The sage dots replacing emojis isn't great either. Go with clean editorial cookbook layout:
- No decorative icons/glyphs at all
- Left-aligned ingredient name, right-aligned amount+unit
- Thin bottom border between rows
- Clean, minimal, like a real cookbook page
- File: `app/recipe/[id]/index.tsx`

### 15. Ingredient density + equivalents JSON file
Create a static JSON file at `lib/ingredient-data.json` with two sections:

**Densities (~350 entries)** — `g_per_cup` values for volume↔weight conversion:
- Baking staples (flours, sugars, cocoa, baking powder, yeast)
- Dairy (butter, cream, milk, yogurt, cheeses)
- Proteins (chicken, beef, fish — less relevant for conversion but include)
- Produce (diced onion, chopped tomatoes, berries, leafy greens)
- Oils and liquids (olive oil, vegetable oil, honey, syrups)
- Grains and pasta (rice, oats, couscous)
- Nuts and seeds
- Spices (ground vs whole)
- Canned goods (crushed tomatoes, beans, coconut milk)

**Count equivalents (~50 entries)** — for count-based units (cloves, heads, stalks, etc.):
- garlic clove → ~5g / ~1 tsp minced
- lemon → ~3 tbsp juice / ~1 tbsp zest
- lime → ~2 tbsp juice
- egg (large) → ~50g / ~3 tbsp
- banana → ~120g / ~½ cup mashed
- onion (medium) → ~150g / ~1 cup diced
- tomato (medium) → ~150g
- avocado → ~150g / ~⅔ cup
- ginger (1 inch) → ~6g / ~1 tbsp grated
- etc.

No API dependency, zero latency, ships with app.
- File: new `lib/ingredient-data.json`

### 16. Unit conversion logic
Create `lib/unit-conversion.ts`:
- Core function: `convert(amount, fromUnit, toUnit, ingredientName?) → number | null`
- **Volume↔volume** is pure math (1 cup = 16 tbsp = 48 tsp = 236.6 ml, 1 quart = 4 cups, 1 gallon = 4 quarts, 1 liter = 1000 ml, 1 fl oz = 29.6 ml)
- **Weight↔weight** is pure math (1 lb = 16 oz = 453.6 g, 1 kg = 1000 g)
- **Volume↔weight** requires density lookup from ingredient-data.json
- **Count↔volume/weight** requires count equivalents lookup (e.g., "3 cloves garlic" → "15g" → "1 tbsp minced")
- Returns null if conversion not possible (no data for that ingredient)
- User preference stored in Supabase `profiles` table (new column `preferred_units: 'metric' | 'us'`, default 'us')

### 17. Tap-to-convert UX on ingredient rows
On recipe detail, each ingredient row is tappable. Tap → toggles between unit systems (e.g., "200g" → "~1.6 cups" → "200g"). Show a small "≈" prefix on converted values to indicate approximation. Default display uses user's preferred unit system from settings.
- File: `app/recipe/[id]/index.tsx`

## Seed Data

### 18. Expand seed recipes — all categories with images
Add more recipes to the becipe account covering full range. ALL must have cover images. Categories:
- Desserts & baking (cookies, cakes, brownies, pies, pastries, bread)
- Breakfast (pancakes, eggs, smoothie bowls, oatmeal)
- Snacks & appetizers (dips, small plates)
- Drinks (cocktails, mocktails, smoothies)
- International variety (not just American/Italian — add Thai, Mexican, Indian, Japanese, Middle Eastern, etc.)
- Remove/replace any existing recipes that lack cover images
- Goal: enough variety that the app feels full and real for new users
